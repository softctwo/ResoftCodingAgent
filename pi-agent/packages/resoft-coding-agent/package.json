{
  "name": "@resoft/coding-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "resoft": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsgo -p tsconfig.build.json",
    "test": "vitest run",
    "lint": "tsgo --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "@resoft/agent-core": "*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^1.6.0"
  }
}
